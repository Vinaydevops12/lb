name: vinay first workflow
on:
  workflow_dispatch:
env:
  cloud: google-clod
jobs:
  greetings_jobs:
    runs-on: ubuntu-latest
    env:
      Greeting: Hello
    steps:
      - name: "Say Hello Vinay Its Sunday"
        run: echo "$Greeting $First_name is using $cloud"
        env:
          First_name: Vinay Kumar
    
